// conetext is used to manage the state (state management)
import { createContext } from "react";
// const UserConetext = React.createContext();
const UserContext = createContext();
export default UserContext;
